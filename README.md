# 大学搜题酱 - Safari Extension

自动转换自 Chrome 版本。

## 推送说明

```bash
git init -b main
git add .
git commit -m "Add extension source and Safari conversion workflow"
git remote add origin https://github.com/qi-1021/college-webclass-manager-safari.git
git push -u origin main
```
